## Platform: local

For local software, the deliverable is local files and executable behavior. Child tasks run in
isolated repo snapshots; inspect their transcripts and evidence, then extract
`workspace.tar.gz` checkpoints to integrate results, including git state.
