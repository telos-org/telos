load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "executor",
    srcs = [
        "executor.go",
        "httpclient.go",
        "loop.go",
        "provider.go",
        "sessionlog.go",
        "tools.go",
        "transport_anthropic.go",
        "transport_chat.go",
        "transport_responses.go",
    ],
    importpath = "github.com/telos-org/telos/internal/executor",
    visibility = ["//:__subpackages__"],
    deps = [
        "//internal/game",
        "//internal/platform",
    ],
)

go_test(
    name = "executor_test",
    srcs = ["native_test.go"],
    embed = [":executor"],
    deps = [
        "//internal/game",
        "//internal/platform",
    ],
)
