# Telos As A Harbor Agent

Harbor agent integration is no longer owned by this repository. Telos should be
treated as a released executable or local build that an evals repository invokes
through the public `telos run` CLI contract.
